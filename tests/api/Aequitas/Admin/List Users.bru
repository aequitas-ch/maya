meta {
  name: List Users
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/users/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
