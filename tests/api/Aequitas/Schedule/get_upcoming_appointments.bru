meta {
  name: get_upcoming_appointments
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/schedule/appointments/upcoming/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}
