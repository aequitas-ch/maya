meta {
  name: Register User Negative (Duplicate)
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/users/register/
  body: json
  auth: none
}

body:json {
  {
    "username": "{{test_username}}",
    "email": "{{test_email}}",
    "first_name": "API",
    "last_name": "Tester",
    "password": "{{test_password}}",
    "display_name": "API Tester Display"
  }
}

assert {
  res.status: eq 400
}
